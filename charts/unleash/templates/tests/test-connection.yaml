apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unleash.fullname" . }}-test-connection"
  labels:
    {{- include "unleash.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test
      image: curlimages/curl
      command: 
        - sh
        - -exc
      args: 
        - sleep 10;curl --head --silent --show-error --retry-all-errors --retry 120 --retry-delay 1 {{ include "unleash.fullname" . }}:{{ .Values.service.port }}
  restartPolicy: Never
