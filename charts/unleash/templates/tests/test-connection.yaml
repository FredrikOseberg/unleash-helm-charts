apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unleash.fullname" . }}-test-connection"
  labels:
    {{- include "unleash.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test
      image: curlimages/curl
      command: 
        - sh
        - -xc
      args: 
        - sleep 10
        - curl {{ include "unleash.fullname" . }}:{{ .Values.service.port }}
  restartPolicy: Never
