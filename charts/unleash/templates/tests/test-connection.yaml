apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unleash.fullname" . }}-test-connection"
  labels:
    {{- include "unleash.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: curlimages/curl
      command: 
        - sh
        - -c
      args: 
        #- sleep 60
        - wget {{ include "unleash.fullname" . }}:{{ .Values.service.port }}
  restartPolicy: Never
